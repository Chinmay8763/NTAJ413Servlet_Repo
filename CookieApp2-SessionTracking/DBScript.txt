CREATE TABLE "SCOTT"."COOKIE_PERSON_INFO" 
   (	"PID" NUMBER(10,0) NOT NULL ENABLE, 
	"PNAME" VARCHAR2(15 BYTE), 
	"PFNAME" VARCHAR2(15 BYTE), 
	"GENDER" VARCHAR2(10 BYTE), 
	"INCOME" FLOAT(126), 
	"TAX" FLOAT(126), 
	 CONSTRAINT "COOKIE_PERSON_INFO_PK" PRIMARY KEY ("PID")
	 
	 CREATE SEQUENCE  "SCOTT"."COOKIE_PID"  
	 MINVALUE 1 MAXVALUE 10000 
	 INCREMENT BY 1 START WITH 1 CACHE 20 
	 NOORDER  NOCYCLE ;